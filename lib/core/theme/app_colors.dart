import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors (Orange & White)
  static const Color primary = Color(0xFFE80A58); // Vibrant Orange-Red
  static const Color primaryDark = Color(0xFFC41C00);
  static const Color accent = Color(0xFFFF9100); // Lighter Orange

  // Backgrounds (Light Mode)
  static const Color background = Color(0xFFFAFAFA); // Off-white
  static const Color surface = Color(0xFFFFFFFF); // Pure white
  static const Color surfaceAlt = Color(0xFFF5F5F7); // Light Grey

  // Status
  static const Color success = Color(0xFF00C853);
  static const Color error = Color(0xFFFF3D00);
  static const Color warning = Color(0xFFFFD600);

  // Text
  static const Color textPrimary = Color(0xFF1D1D1F); // Nearly black
  static const Color textSecondary = Color(0xFF86868B);
  static const Color textDisabled = Color(0xFFD2D2D7);

  // Extras
  static const Color border = Color(0xFFE5E5EA);
}
